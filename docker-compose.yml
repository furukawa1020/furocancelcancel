version: '3.8'

services:
  backend:
    build: ./backend-node
    ports:
      - "3000:3000"
    volumes:
      - ./backend-node/database.sqlite:/app/database.sqlite
    environment:
      - PORT=3000
  # Frontend would typically be built and served by Nginx or similar in prod,
  # or standard Vite dev server in dev.
  # For simplicity, we just define backend here for the API service.
